all-local:
	cp -dR --no-preserve=mode --preserve=timestamps --no-target-directory --update --reflink=auto $(srcdir)/daala.upstream ./daala && cd daala && $(MAKE) $(AM_MAKEFLAGS)

check-local:

clean-local:
	rm -rf daala

install-data-local:

install-exec-local:

uninstall-local:

EXTRA_DIST = daala.upstream
